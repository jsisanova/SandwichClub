<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/backgroundColor</item>
        <item name="android:textColor">@color/whiteColor</item>

        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/MyActionBar</item>
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar" parent="@style/Widget.AppCompat.ActionBar">
        <item name="android:titleTextStyle">@style/MyActionBarTitleText</item>
        <!-- Support library compatibility -->
        <item name="titleTextStyle">@style/MyActionBarTitleText</item>

        <!--ActionBar color-->
        <item name="android:background">@color/colorPrimaryDark</item>
        <!-- Support library compatibility -->
        <item name="background">@color/colorPrimaryDark</item>
    </style>

    <!-- ActionBar title text -->
    <style name="MyActionBarTitleText"
        parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/backgroundColor</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <!--Style for labels in Detail layout-->
    <style name="DetailLabelStyle">
        <item name="android:paddingTop">@dimen/paddingGeneral</item>
        <item name="android:paddingRight">@dimen/paddingGeneral</item>
        <item name="android:paddingLeft">@dimen/paddingGeneral</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:fontFamily">monospace</item>
    </style>

    <!--Style for plain text in Detail layout-->
    <style name="DetailTextStyle">
        <item name="android:paddingTop">@dimen/paddingTopDetailText</item>
        <item name="android:paddingRight">@dimen/paddingGeneral</item>
        <item name="android:paddingLeft">@dimen/paddingGeneral</item>
        <item name="android:textSize">15dp</item>
    </style>
</resources>